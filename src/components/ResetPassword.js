import React from "react";

const ResetPassword = () => {
  return (
    <div>
      <input placeholder="reset password..." />
      <button>reset</button>
    </div>
  );
};

export default ResetPassword;
